Run this app step guide

1. Open this into vscode
2. install packages by npm i or npm install
3. setup your mongodb url in .env file
3. Run this project by "npm run dev"

This app based on mern post crud with authentication api build in nodejs (backend)